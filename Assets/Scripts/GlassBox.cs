using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlassBox : Boxes
{
    // Start is called before the first frame update
    void Start()
    {
        HP = 0;
    }

    // Update is called once per frame

}
