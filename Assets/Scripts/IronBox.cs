using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// INHERITANCE
public class IronBox : Boxes
{
    // Start is called before the first frame update
    void Start()
    {
        // POLYMORPHISM
        HP = 5;
    }

    // Update is called once per frame

}
