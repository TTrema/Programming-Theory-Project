using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// INHERITANCE
public class WoodBox : Boxes
{

    // Start is called before the first frame update
    void Start()
    {
        // POLYMORPHISM
        HP = 3;
    }

    // Update is called once per frame

}
